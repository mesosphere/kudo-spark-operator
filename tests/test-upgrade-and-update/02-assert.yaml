apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 120
commands:
  - script: |
      set -eux

      test $(kubectl get pod -n $NAMESPACE -l app.kubernetes.io/instance=spark -o=jsonpath='{.items[].spec.containers[].args[]}') = "-v=3"
